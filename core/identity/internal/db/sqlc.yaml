version: "2"
sql:
  - schema: "migrations"
    queries: "queries"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "gen"
        emit_interface: true
        sql_package: "pgx/v5"
